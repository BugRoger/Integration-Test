<?xml version="1.0" encoding="utf-8" ?>
<project name="integration-test" default="compile" basedir=".">  
    <!-- = Set to true if this module is not generating a deployable artifact = -->
    <property name="project.name" value="integration-test" />

    <!-- = Set to true to execute build with production seetings = -->
	<property name="build.gwt.options.isproduction" value="false" /> 

    <!-- = Set to true if this module is not generating a deployable artifact = -->
    <property name="build.gwt.options.isheadless" value="false" />

	<!-- ==================================================== -->
	<!-- = DO NOT EDIT BELOW THIS LINE                        = -->
	<!-- ==================================================== -->
    <import file="build.gwt.xml" />
</project>
 
 
